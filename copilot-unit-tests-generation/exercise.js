// exercise.js
// Day 2: Use Copilot to generate a Jest test for this function.
function calculateTotalPrice(items) {
  return items.reduce((total, item) => total + item.price, 0);
}
module.exports = calculateTotalPrice;
